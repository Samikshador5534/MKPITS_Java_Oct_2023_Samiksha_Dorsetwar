let value = prompt("enter a size of an array");
let arr = [];
let size = prompt("enter a size");
for (i = 0; i <= size; i++) {
  let arrelement = prompt("enter a $ (i +1) element for the array: ");
  arr.push(arrelement);
}
console.log(arr);
