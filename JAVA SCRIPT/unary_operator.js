//The Unary plus(+) operation is a single operand operator
//(which means it worked with only a single operand preceding or succeeding to it),
// which is used to convert its operand to a number, if it isn’t already a number.

const x = "10";
let y;
y = +x;
console.log(y);
console.log(typeof y);

const a = 100;
const b = -100;
const c = 20;

console.log(+a);
console.log(+b);
console.log(+c);
